<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:util="http://www.springframework.org/schema/util" xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">

    <!-- 开启定时任务 -->
    <task:annotation-driven />

    <mvc:annotation-driven/>

    <context:component-scan base-package="code.*"/>

    <mvc:resources mapping="/images/**" location="/images/" />
    <mvc:resources mapping="/css/**" location="/css/" />
    <mvc:resources mapping="/js/**" location="/js/" />
    <mvc:resources mapping="/htmls/**" location="/htmls/"/>

    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/view/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 使用注解注入properties中的值 -->
    <util:properties id="mysettings" location="classpath:mysettings.properties" />
    <bean id="props" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:timerConf.properties</value>
                <value>classpath:jedisConf.properties</value>
            </list>
        </property>
    </bean>

    <bean class="code.utils.JSONUtils">
    </bean>

    <!-- 拦截器 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/manag/**"/>
            <mvc:exclude-mapping path="/manag/login"/> <!-- 无需拦截的请求 -->
            <bean class="code.utils.ManagerInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 使用 spring 创建爬虫 -->
    <bean id="CSDNSearchPageProcesser" class="code.utils.CsdnBlogCrawler" scope="singleton">
        <constructor-arg ref="managerDao"/>
    </bean>
    <bean class="code.utils.MySpider" scope="singleton">
        <constructor-arg ref="CSDNSearchPageProcesser"/>
    </bean>

</beans>